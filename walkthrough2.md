# دليل وخطوات تجهيز مشروع لارافل للرفع (Deployment Guide)

تم التحقق من جاهزية المشروع بالكامل للرفع دون أية مشاكل برمجية أو أخطاء في التكوين. يوضح هذا الملف تفاصيل الفحص البرمجي الذي تم، بالإضافة إلى دليل خطوة بخطوة للرفع والتشغيل بشكل آمن وصحيح.

---

## 1. نتائج الفحص والتحقق المحلي (Local Verification Results)

قمنا بتشغيل العمليات والتحقق منها عبر سطر الأوامر (Terminal) محلياً للتأكد من خلو المشروع من أخطاء الصياغة (Syntax Errors) أو تعارضات التوجيه (Routing Conflicts):

1. **التحقق من الكاش (Caching Commands):**
   تم تشغيل أوامر الكاش التالية بنجاح وبدون أية استثناءات:
   * `php artisan config:cache` (تم بنجاح)
   * `php artisan route:cache` (تم بنجاح)
   * `php artisan view:cache` (تم بنجاح)
   
   *ملاحظة:* تم إعادة تنظيف الكاش محلياً بعد التحقق عبر أوامر `clear` المقابلة لضمان استمرارية بيئة التطوير المحلية بسلاسة.

2. **الاختبارات الآلية (Automated Tests):**
   تم تشغيل جميع الاختبارات الخاصة بالمشروع (`php artisan test`)، وكانت النتيجة **مرور جميع الاختبارات الـ 29 بنجاح** (مع تخطي اختبار واحد متعلق بالبحث القريب بسبب استخدام SQLite محلياً وعدم دعمها لدوال الجيب وجيب التمام الرياضية، وهو سلوك طبيعي وسيعمل الاختبار بنجاح في بيئة الإنتاج عند استخدام MySQL أو PostgreSQL).

3. **توافق الحزم (Composer Lock):**
   الملف `composer.json` و `composer.lock` متطابقان تماماً وجاهزان لتثبيت الحزم المخصصة للإنتاج (Production-ready packages).

---

## 2. خطوات تهيئة وإعداد الاستضافة (Deployment Dashboard Configuration)

عند رفع الكود إلى GitHub ومن ثم ربطه بمنصة الاستضافة (مثل **Railway** أو **Koyeb**)، يجب ضبط الإعدادات التالية في لوحة تحكم المنصة لضمان أمان عمل الباك إيند وقاعدة البيانات:

### أ. إعدادات البيئة (Environment Variables)
> [!IMPORTANT]
> لا ترفع ملف `.env` المحلي إلى GitHub (هو مستثنى تلقائياً عبر ملف `.gitignore`). بدلاً من ذلك، قم بإضافة المتغيرات التالية مباشرة في قسم الـ **Environment Variables** في منصة الاستضافة:

| اسم المتغير (Key) | القيمة المقترحة (Value) | الهدف من المتغير |
| :--- | :--- | :--- |
| **`APP_ENV`** | `production` | تحديد بيئة التشغيل كبيئة إنتاج رسمية. |
| **`APP_DEBUG`** | `false` | **مهم جداً:** إخفاء تفاصيل الأخطاء الحساسة لمنع تسريب بيانات السيرفر للمستخدمين. |
| **`APP_URL`** | `https://your-domain.koyeb.app` | رابط موقعك أو الدومين الخاص بالسيرفر الجديد. |
| **`APP_KEY`** | `base64:oK5kzK67QkScYLQt3DEA0+pSKQ3KfZPRPH7h5srG5H4=` | مفتاح التشفير الخاص بالتطبيق (يمكنك استخدام نفس المفتاح المحلي أو توليد مفتاح جديد). |
| **`JWT_SECRET`** | `Fw0Kfdwh4J3aP7jB4yB5WZNABq8JioeilDFSWGDM3rUTorkmXkFjZWxK68et43Kw` | المفتاح السري لتوقيع توكنات تسجيل الدخول (JWT Tokens). |
| **`DB_CONNECTION`** | `mysql` | نوع محرك قاعدة البيانات المستخدم في الإنتاج. |
| **`DB_HOST`** | *من بيانات قاعدة البيانات المستضافة* | عنوان سيرفر قاعدة البيانات. |
| **`DB_PORT`** | `3306` | منفذ اتصال MySQL. |
| **`DB_DATABASE`** | *اسم قاعدة البيانات التي أنشأتها* | اسم قاعدة البيانات الإنتاجية. |
| **`DB_USERNAME`** | *اسم مستخدم قاعدة البيانات* | اسم المستخدم للاتصال بالقاعدة. |
| **`DB_PASSWORD`** | *كلمة مرور قاعدة البيانات* | كلمة المرور الخاصة بقاعدة البيانات. |

---

### ب. أوامر البناء (Build Commands)

* **للرفع والتشغيل على منصات الاستضافة (Railway / Koyeb):**
  اختر نظام التشغيل (Linux/Bash) وضعي الأمر التالي في إعدادات البناء:
  ```bash
  composer install --no-dev --optimize-autoloader && php artisan config:cache && php artisan route:cache && php artisan view:cache
  ```

* **للتجربة والاختبار محلياً (Windows PowerShell):**
  بعد أن قمت بتفعيل مكتبة `gd` بنجاح، تحتاج فقط لتشغيل الأمر التالي مع تجاهل قيد إصدار PHP محلياً (لأن لديك PHP 8.5.1):
  ```powershell
  composer install --no-dev --optimize-autoloader --ignore-platform-req=php; php artisan config:cache; php artisan route:cache; php artisan view:cache
  ```

---

### ج. أمر الهجرة التلقائي (Migration Run Command)
لتشغيل الجداول وبنائها تلقائياً عند كل عملية رفع جديدة، أضف الأمر التالي في حقل **Release Command** أو **Pre-run Command** أو **Start Command** (حسب المنصة المستضيفة):

```bash
php artisan migrate --force
```

> [!WARNING]
> استخدام الخيار `--force` ضروري جداً لتخطي تأكيدات التشغيل التفاعلية في بيئة الإنتاج (`production`).

---

## 3. نصائح إضافية لضمان عمل مستقر ومجاني
1. **أداء قاعدة البيانات المجانية:** تمنحك منصات مثل Railway أو Koyeb قواعد بيانات مجانية، تأكد من إنشاء قاعدة البيانات في نفس المنطقة الجغرافية (Region) الخاصة بتطبيق Laravel لتقليل زمن الاستجابة (Latency).
2. **سعة التخزين (Cloud Storage / S3):**
   بما أن منصات الاستضافة السحابية تستخدم أقراصاً مؤقتة (Ephemeral Storage) يتم مسح ملفاتها عند كل إعادة تشغيل أو عملية بناء جديدة، **لقد قمنا بتثبيت الحزمة اللازمة للاتصال بسحابة التخزين (`league/flysystem-aws-s3-v3`)**.
   
   لتشغيل التخزين السحابي (مثل AWS S3 أو MinIO أو Cloudflare R2) لحفظ صور الأدوية بشكل دائم، كل ما عليك فعله هو إضافة المتغيرات التالية في لوحة تحكم الاستضافة:
   
   * **`MEDIA_DISK`**: `s3` (لتوجيه مكتبة الميديا لاستخدام السحابة بدلاً من التخزين المحلي).
   * **`AWS_ACCESS_KEY_ID`**: *مفتاح الوصول الخاص بحساب السحابة*
   * **`AWS_SECRET_ACCESS_KEY`**: *المفتاح السري الخاص بحساب السحابة*
   * **`AWS_DEFAULT_REGION`**: `us-east-1` (أو المنطقة الجغرافية للسيرفر الخاص بك)
   * **`AWS_BUCKET`**: *اسم الحاوية (Bucket)*
   * **`AWS_ENDPOINT`**: *رابط الـ Endpoint (في حال استخدام MinIO أو Cloudflare R2 أو Liara)*
   * **`AWS_USE_PATH_STYLE_ENDPOINT`**: `true` (مطلب لبعض مقدمي الخدمة مثل MinIO)
