# توثيق وشرح كود الخلفية (API) لمشروع دواء.كوم (DEPI-Back)

يحتوي هذا الملف على شرح متكامل لكافة مجلدات وملفات مشروع الخلفية المطور باستخدام إطار عمل **Laravel 11** وبنية الـ **RESTful API**، مع توضيح وظيفة كل ملف بالتفصيل وخطوات التهيئة والتشغيل من الصفر.

---

## 1. الهيكل العام للمجلدات (Project Directory Structure)

```text
DEPI-Back (final round)1/
├── app/                      # المجلد الأساسي لمنطق التطبيق
│   ├── Http/
│   │   ├── Controllers/Api/  # المتحكمات (Controllers) التي تعالج طلبات الـ API
│   │   └── Middleware/       # البرمجيات الوسيطة (مثل التحقق من الرتب واللغات)
│   ├── Models/               # النماذج (Models) التي تمثل جداول قاعدة البيانات وعلاقاتها
│   └── Providers/            # مزودو الخدمات لتهيئة إعدادات إطار العمل
├── bootstrap/                # ملفات تشغيل لارافيل ووسائط التسجيل
├── config/                   # ملفات الإعدادات والتكوين (قاعدة البيانات، البريد، الحماية)
├── database/                 # تهجير الجداول وبذر البيانات الافتراضية
│   ├── factories/            # مصانع لتوليد بيانات وهمية للاختبارات
│   ├── migrations/           # جداول قاعدة البيانات وعلاقاتها البرمجية
│   └── seeders/              # البيانات الأساسية والتجريبية المدخلة مسبقاً
├── routes/                   # ملفات التوجيه وتعريف روابط الـ API
│   ├── api.php               # روابط الـ API الرئيسية للتطبيق (موضع العمل)
│   └── web.php               # الروابط التقليدية لعرض الصفحات
├── storage/                  # الملفات المرفوعة، ملفات سجل الأخطاء، وكاش النظام
├── .env.example              # ملف بيئة تجريبي يوضح المتغيرات المطلوبة للتشغيل
├── artisan                   # أداة سطر الأوامر الخاصة بلارافيل لإدارة المشروع
├── composer.json             # ملف إدارة المكتبات والحزم الخاصة بلغة PHP
├── vite.config.js            # ملف إعدادات أداة تجميع ملفات الواجهة Vite
└── notes.md                  # هذا الملف (توثيق المشروع)
```

---

## 2. جدول شرح ملفات التكوين والإعدادات في جذر المشروع

| اسم الملف | نوع الملف | شرح عمل الملف والخطوات البرمجية فيه |
| :--- | :--- | :--- |
| **`composer.json`** | JSON | يدير حزم PHP والمكتبات التي يعتمد عليها المشروع (مثل حزم Spatie لإدارة الصلاحيات وحزم JWT للمصادقة وتأمين الجلسات). |
| **`package.json`** | JSON | يدير حزم JavaScript والأدوات المساعدة لعمليات البناء (مثل Vite وتنسيقات Tailwind) إذا تم تفعيلها. |
| **`vite.config.js`** | JS | أداة بناء وتجميع سريعة لملفات الـ CSS والـ JS، وتحديث المتصفح تلقائياً أثناء التطوير. |
| **`.env`** | Text | يحتوي على إعدادات بيئة العمل الحالية الحساسة (مثل اسم قاعدة البيانات، المستضيف، اسم المستخدم، البريد الإلكتروني، والمفاتيح السرية). |
| **`.env.example`** | Text | نسخة نموذجية لملف بيئة العمل بدون القيم الحساسة، يستعين بها المطور الجديد لبناء ملف `.env` الخاص به. |
| **`artisan`** | PHP | أداة التحكم والتشغيل بلارافيل عبر سطر الأوامر (الـ CLI)، لعمل تهجير للجداول، وتوليد الأكواد، وبذر البيانات. |
| **`phpunit.xml`** | XML | ملف إعدادات الاختبارات الآلية والوحداتية (Unit & Feature Tests) لضمان جودة الأكواد. |

---

## 3. شرح مجلدات التكوين والتهيئة (`bootstrap/` & `config/`)

### مجلد `bootstrap/`:
- **`app.php`**: يقوم بتهيئة تطبيق لارافيل وتسجيل البرمجيات الوسيطة (Middleware) مثل وسيط ضبط اللغة `set_locale` ووسيط التحقق من الرتب `role` ومعالجة الاستثناءات والأخطاء.
- **`providers.php`**: يسجل مزودي الخدمة (Service Providers) لضمان تشغيل الوحدات بشكل سليم.

### مجلد `config/`:
يحتوي على إعدادات تفصيلية لكل ميزة في لارافيل:
- `database.php`: إعدادات الاتصال بمختلف قواعد البيانات (MySQL, PostgreSQL, SQLite).
- `auth.php`: إعدادات الحماية وتعريف خراس المصادقة (Guards) حيث تم تخصيص `api` للعمل بنظام `jwt` بدلاً من الجلسات التقليدية.
- `cors.php`: يحدد النطاقات المسموح لها بالاتصال بالـ API (Cross-Origin Resource Sharing) لحماية البيانات.
- `app.php`: إعدادات اللغة الافتراضية، المنطقة الزمنية، واسم التطبيق.

---

## 4. شرح مسارات وتوجيه الطلبات (`routes/`)

- **`api.php`**:
  الملف الرئيسي الذي يستقبل كافة طلبات الفرونت إند وتطبيق الموبايل. يتم حماية المسارات الحساسة فيه باستخدام وسيط `auth:api`.
  - **مسارات عامة**: التسجيل ودخول المستخدمين، جلب الدول، المحافظات، المدن، التصنيفات، الأدوية، والصيدليات.
  - **مسارات محمية (للمستخدمين)**: تفاصيل الملف الشخصي، الإشعارات وتأكيد القراءة، والبحث القريب.
  - **مسارات أصحاب الصيدليات**: التحكم بالأدوية التابعة لهم، إدارة الفروع، تنبيهات المخزون المتدني، والانتهاء الوشيك للصلاحية.
  - **مسارات مدير النظام (Super Admin)**: إدارة الحسابات بالكامل، تفعيل الصيدليات، والتحكم بالبيانات الجغرافية (إضافة دول ومدن ومحافظات جديدة).

---

## 5. شرح النماذج والعلاقات البرمجية (`app/Models/`)

تمثل النماذج جداول قاعدة البيانات وتوضح العلاقات المنطقية والرياضية بينها:
- **`User.php`**: يمثل جدول المستخدمين. يحتوي على التحقق من رتب المستخدمين (`isSuperAdmin`, `isPharmacyOwner`, `isClient`). يرتبط بصيدلية محددة (علاقة `belongsTo`) وفرع محدد. يطبق حزمة Spatie للصلاحيات وحزمة JWT للمصادقة.
- **`Pharmacy.php`**: يمثل جدول الصيدليات. يحتوي على حقل الاسم متعدد اللغات (مترجم للغتين). يرتبط بالعديد من الفروع (`hasMany`) والمستخدمين التابعين لها.
- **`Branch.php`**: يمثل جدول الفروع. يرتبط بصيدلية رئيسية (`belongsTo`) وبمدينة محددة (`belongsTo`). يحتوي على علاقة متعدد لمتعدد (`belongsToMany`) مع الأدوية لتمثيل كمية المخزون وتاريخ انتهاء الصلاحية لكل دواء في الفرع عبر جدول وسيط.
- **`Medicine.php`**: يمثل جدول الأدوية. يحتوي على الأسعار، المواد الفعالة، الاستخدام، والأعراض الجانبية. يرتبط بقسم رئيسي (`belongsTo`) وبالعديد من فروع الصيدليات.
- **`Category.php`**: يمثل جدول أقسام الأدوية (مثل: المسكنات، المضادات الحيوية). يرتبط بالعديد من الأدوية (`hasMany`).
- **`Country.php`, `Governorate.php`, `City.php`**: تمثل البنية الجغرافية الهرمية للموقع لتسهيل الفلترة الجغرافية (الدولة تحتوي محافظات، والمحافظة تحتوي مدن، والمدينة تحتوي فروع صيدليات).

---

## 6. شرح متحكمات الـ API (`app/Http/Controllers/Api/`)

تستقبل الطلبات وتقوم بالتحقق من البيانات وإجراء منطق العمل البرمجي ثم إعادة النتيجة كاستجابة JSON:
- **`Auth/AuthController.php`**: مسؤول عن التحقق من حساب العميل أو مالك الصيدلية وصحة بياناته، وتسجيله، وإصدار التوكن JWT، وإدارة معاملات قاعدة البيانات (Database Transactions) لضمان عدم إنشاء بيانات تالفة عند التسجيل.
- **`MedicineController.php`**: يدير عمليات البحث المتقدم والتصفية للأدوية حسب السعر والتصنيف، بالإضافة لعمليات الإضافة والتعديل والحذف للأدوية.
- **`PharmacyController.php`** و **`BranchController.php`**: يديران عرض وتحديث بيانات الصيدليات وفروعها الجغرافية وإجراء استعلامات جغرافية لجلب الفروع القريبة من المستخدم.
- **`NotificationController.php`**: يدير إرسال الإشعارات والتنبيهات للمستخدمين أو للمالكين (مثل تنبيهات نقص المخزون).
- **`UserController.php`**: يدير معلومات الحسابات الشخصية وعمليات التعديل عليها من قبل المستخدمين أو مدير النظام.
- **`DashboardController.php`**: جلب الإحصائيات والأرقام التلخيصية لعرضها في لوحات التحكم.

---

## 7. خطوات عمل وبناء الخلفية من الصفر

لتشغيل وتطوير مشروع الباك إند محلياً، يرجى اتباع الخطوات التالية:

### الخطوة 1: تثبيت بيئة PHP و Composer
يجب التأكد من تثبيت **PHP** (إصدار 8.2 أو أحدث) وأداة إدارة الحزم **Composer** على جهازك.

### الخطوة 2: تثبيت الحزم والمكتبات
افتح سطر الأوامر في مجلد المشروع ونفذ الأمر التالي لتحميل كافة الحزم المطلوبة:
```bash
composer install
```

### الخطوة 3: إنشاء ملف البيئة وتوليد المفاتيح
قم بأخذ نسخة من ملف النموذج وتسميتها `.env`:
```bash
copy .env.example .env
```
ثم قم بتوليد مفتاح تشفير التطبيق ومفتاح حماية JWT:
```bash
php artisan key:generate
php artisan jwt:secret
```

### الخطوة 4: تهيئة قاعدة البيانات
قم بإنشاء قاعدة بيانات فارغة (مثلاً MySQL)، واكتب بيانات الاتصال بها في ملف `.env` (مثل `DB_DATABASE` و `DB_USERNAME`).
ثم قم بتشغيل التهجير لبناء الجداول وبذر البيانات التجريبية الأساسية:
```bash
php artisan migrate --seed
```

### الخطوة 5: ربط مجلد التخزين (Storage Link)
لتمكين خادم الويب من خدمة وعرض الصور المرفوعة من الصيدليات:
```bash
php artisan storage:link
```

### الخطوة 6: تشغيل السيرفر المحلي
لتشغيل خادم التطوير الداخلي للارافيل:
```bash
php artisan serve
```
سيكون خادم الباك إند متاحاً افتراضياً على الرابط: `http://127.0.0.1:8000`
يمكنك التحقق من الـ API بطلب الرابط: `http://127.0.0.1:8000/api/v1/medicines`
